extend schema @link(url: "https://specs.apollo.dev/federation/v2.7", import: ["@key", "@interfaceObject"])

type MediaItem @key(fields: "id") @interfaceObject {
  id: ID!
  averageRating: Float
  reviewCount: Int!
}
